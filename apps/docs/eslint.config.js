import { nextJsConfig } from '@workspace/eslint-config/next-js';

/** @type {import("eslint").Linter.Config} */
export default [
  ...nextJsConfig,
  {
    // Generated by `fumadocs-mdx` from the content tree — not authored here.
    ignores: ['.source/**'],
  },
];
