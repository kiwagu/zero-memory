---
description: Brief me from memory — stored context, rules and open loops for a topic
argument-hint: [topic]
---

Call build_context on the zero-memory server with briefing: true and topic
$ARGUMENTS (when no topic was given, use "the current project and its active
work"). Then report: the standing rules[] (obey them), any open_loops[]
(recorded open work), and the memories most relevant right now. Where a hit
is truncated, read its zm://memory/{id} resource for the full text before
quoting it.
