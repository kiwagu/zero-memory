import 'reflect-metadata';

export {
  container,
  inject,
  injectable,
  instanceCachingFactory,
  Lifecycle,
  registry,
  singleton,
} from 'tsyringe';
export type {
  ClassProvider,
  DependencyContainer,
  FactoryProvider,
  InjectionToken,
  Provider,
  ValueProvider,
} from 'tsyringe';
